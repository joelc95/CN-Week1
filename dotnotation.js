// assign string to variable
let myName = 'Joel';
// access string stored in variable and print to terminal
console.log(myName);
// access length property of variable
console.log(myName.length);
// perform lowercase method on variable
console.log(myName.toLowerCase());
// access Math library; generates a float between 0-1
console.log(Math.random());
// round output of random (multiplied by 6) up to int
console.log(Math.ceil(Math.random()*6));
